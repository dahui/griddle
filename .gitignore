# Rust
/target/

# JS
node_modules/
dist/
*.tsbuildinfo

# Astro's build cache and generated types, under docs/. `dist/` above already covers its output.
.astro/

# Tauri
/crates/griddle-app/gen/

# Secrets. The SteamGridDB API key is a PER-USER secret — every API endpoint 401s without
# one, so a key WILL get pasted somewhere during development. Enforced by
# scripts/check-secrets.sh (pre-commit hook + CI), which fails the build rather than trusting
# this list to be exhaustive.
.env
.env.*
!.env.example
*.key
*.secret
secrets.json
**/apikey.txt

# Real Steam fixtures are NEVER committed — they contain home directory paths.
# See crates/griddle-core/tests/real_shortcuts.rs; point SGDB_REAL_SHORTCUTS at the live file.
/crates/griddle-core/fixtures/real/

# Local scratch
/scratch/
*.local
